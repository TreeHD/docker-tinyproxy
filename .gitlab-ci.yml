default:
  image: docker:27.0
  services:
    - docker:27.0-dind

stages:
  - build

variables:
  # Empty tag will become the tag "latest".
  IMAGE_TAG: ""

build:
  stage: build
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" "$CI_REGISTRY"
    - docker run --rm --privileged multiarch/qemu-user-static --reset -p yes
    - docker buildx create --use
  script:
    # If not in master, use branch name with any ".x" suffixes removed.
    - |
      if [ "$CI_COMMIT_BRANCH" != "$CI_DEFAULT_BRANCH" ]; then
        IMAGE_TAG=":$(echo "$CI_COMMIT_REF_NAME" | sed 's/\.x$//')"
      fi
    - docker buildx build --platform linux/amd64,linux/arm64,linux/arm/v7 --pull --push -t "${CI_REGISTRY_IMAGE}${IMAGE_TAG}" .
